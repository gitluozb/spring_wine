spring:
  application:
    name: web-wine
  cloud:
    config:
      discovery:
        service-id: wine-config
        enabled: true
      profile: dev
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/